# Passcode that unlocks the NDA-protected case studies on the home grid.
# Set this in your local .env (gitignored) and in your Vercel project env.
# NOTE: Vite inlines this into the built client bundle, so it is a soft
# deterrent, not real access control — a determined visitor can find it.
VITE_UNLOCK_CODE=your-passcode-here
